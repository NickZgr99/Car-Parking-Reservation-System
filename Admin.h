#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <cstring>
#include <map>
#include <iterator>
#include <list>

#include "User.h"

using namespace std;
/*
class Admin::public User
{
public:
    void delUser(User& user_to_delete);
};

void Admin::delUser(User& user_to_delete)
{
    string temp_user_name = user_to_delete.get_username();
    delete user_to_delete;
    cout << "\nUser " << temp_user_name << " has been deleted successfully";
}
*/
